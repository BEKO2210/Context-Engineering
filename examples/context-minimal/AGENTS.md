# AGENTS.md — Minimal Example

## Identity

A minimal TypeScript module demonstrating context engineering patterns.

## Rules

1. All functions must have explicit return types
2. All public functions must have tests
3. No external dependencies — standard library only
4. ESM imports only

## Structure

- `src/example.ts` — Core module with utility functions
- `tests/example.test.ts` — Test suite
